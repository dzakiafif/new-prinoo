{% extends 'sublayout_frontend.html.twig' %}

{% block content %}
    <section>
        <div class="container">
            <div class="section-content">
                <form method="post">
                <div class="row">
                    <div class="col-md-12">
                        <div class="table-responsive">
                                <table class="table table-striped table-bordered tbl-shopping-cart">
                                    <thead>
                                    <tr>
                                        <th></th>
                                        <th>Photo</th>
                                        <th>Product Name</th>
                                        <th>Price</th>
                                        <th>Quantity</th>
                                        <th>Total</th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    <tr class="cart_item">
                                        <td class="product-remove"><a title="Remove this item" class="remove" href="#">×</a></td>
                                        <td class="product-thumbnail"><a href="#"><img alt="member" src="http://placehold.it/285x300"></a></td>
                                        <td>{{ app.session.get('nama-barang')['value'] }}</td>
                                        <td class="product-price" id="harga">{{ app.session.get('harga-barang')['value'] }}</td>
                                        <td class="product-quantity">
                                            <input id="jumlah" type="number" class="input-text qty text" title="Qty" name="total_pemesan">
                                        </td>
                                        <td class="product-subtotal"><input type="text" name="total_harga" id="total"></td>
                                    </tr>
                                    </tbody>
                                </table>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-12 mt-30">
                            <div class="col-md-6">
                                &nbsp;
                            </div>
                            <div class="col-md-6">
                                <button class="btn btn-primary pull-right">process to checkout</button>
                            </div>
                        </div>
                    </div>
                </div>
                </form>
            </div>
        </div>
    </section>
{% endblock %}
{% block customJS %}
    <script>

        $(function () {

            $('#jumlah').change(function() {

                var harga = parseInt($('#harga').text());

                var jumlah = parseInt($(this).val());


            $('#total').val(harga * jumlah);

            });
        });


    </script>
{% endblock %}